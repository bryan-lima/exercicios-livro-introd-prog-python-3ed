# Modifique o programa anterior para imprimir de 1 até o número digitado pelo usuário, mas, dessa vez, apenas os números ímpares

lastNumber  = int(input('Digite o último número a imprimir: '))
n = 1
while n <= lastNumber:
    print(n)
    n += 2
